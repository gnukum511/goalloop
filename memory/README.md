# memory/

Durable lessons distilled by the Fable 5 review pass, read back by the
planner on every subsequent run. Append-only markdown; the planner reads a
digest capped at 20k chars, so keep entries short and durable (gotchas,
wrong assumptions, escalation patterns) — not run-specific noise.
